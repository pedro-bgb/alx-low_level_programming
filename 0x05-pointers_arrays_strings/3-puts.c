#include "main.h"
#include <stdio.h>

/**
 *  * _puts - Entry point
 *   * @str: variable to be checked
 *    *
 *     * Description: prints char to stdout
 *      *
 *       * Return: Always 0
 */
void _puts(char *str)
{
		puts(str);
}
