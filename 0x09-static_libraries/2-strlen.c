#include "main.h"
#include <string.h>

/**
 *  * _strlen - prints lenght of a string
 *   * @s: variable to be checked
 *    *
 *     * Descriptipn: prints length of strin
 *     *
 *Return: Always 0
 */
int _strlen(char *s)
{
		return (strlen(s));
}
