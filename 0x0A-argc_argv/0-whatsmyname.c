#include <stdlib.h>
#include <stdio.h>

/**
 * main - program that prints its name
 * @argc: the number os argument
 * @argv: array pointer
 *
 * Return: Always 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
